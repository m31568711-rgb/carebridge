import{readFileSync}from'node:fs';
for(const line of readFileSync('.env.local','utf8').split(/\r?\n/)){const m=line.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);if(m&&!process.env[m[1]])process.env[m[1]]=m[2].replace(/^(['"])(.*)\1$/,'$2')}
for(const k of['SUPABASE_PROJECT_REF','SUPABASE_ACCESS_TOKEN'])if(!process.env[k])throw new Error(`Missing ${k}`);
const query=`do $$ declare patient uuid; admin_id uuid; country uuid; city uuid; specialty uuid; h uuid; d uuid; t uuid; lab uuid; rad uuid; property uuid; room uuid; admin_journey uuid; choice_journey uuid; begin
 select id into patient from auth.users where email='demo.patient@carebridge.test'; select user_id into admin_id from public.user_roles where role in('SUPER_ADMIN','ADMIN') order by case when role='SUPER_ADMIN' then 0 else 1 end limit 1; select id into country from public.countries limit 1; select id into city from public.cities where country_id=country limit 1; select id into specialty from public.specialties where status='ACTIVE' limit 1;
 if patient is null or admin_id is null or country is null or city is null or specialty is null then raise exception 'Required demo master data is missing'; end if;
 insert into public.hospitals(legal_name,display_name_i18n,slug,description_i18n,country_id,city_id,status,is_verified,verified_at,verification_state,created_by) values('CareBridge Hospital - TEST','{"en":"CareBridge Hospital - TEST","ar":"مستشفى كيربريدج - اختبار"}','carebridge-hospital-test','{}',country,city,'ACTIVE',true,timezone('utc',now()),'VERIFIED',admin_id) on conflict(slug) do update set status='ACTIVE',is_verified=true,verified_at=timezone('utc',now()),verification_state='VERIFIED' returning id into h;
 select id into d from public.doctors where display_name='Dr Amelia Test' limit 1; if d is null then insert into public.doctors(first_name,last_name,display_name,biography_i18n,status,is_verified,verified_at,verification_state,languages) values('Amelia','Test','Dr Amelia Test','{}','ACTIVE',true,timezone('utc',now()),'VERIFIED',array['en','ar']) returning id into d; end if;
 insert into public.doctor_hospitals(doctor_id,hospital_id,title,is_primary,consultation_available) values(d,h,'Consultant',true,true) on conflict do nothing;
 insert into public.treatments(specialty_id,code,slug,name_i18n,description_i18n,status) values(specialty,'carebridge_test_procedure','carebridge-test-procedure','{"en":"CareBridge Procedure - TEST","ar":"إجراء كيربريدج - اختبار"}','{}','ACTIVE') on conflict(code) do update set status='ACTIVE' returning id into t;
 insert into public.hospital_treatments(hospital_id,treatment_id,starting_price,currency,status) values(h,t,8000,'EUR','ACTIVE') on conflict(hospital_id,branch_id,treatment_id) do update set starting_price=8000,currency='EUR',status='ACTIVE';
 insert into public.medical_laboratories(legal_name,display_name_i18n,slug,description_i18n,country_id,city_id,address_i18n,status,verification_state,is_verified,verified_at) values('CareBridge Laboratory - TEST','{"en":"CareBridge Laboratory - TEST","ar":"مختبر كيربريدج - اختبار"}','carebridge-laboratory-test','{}',country,city,'{}','ACTIVE','VERIFIED',true,timezone('utc',now())) on conflict(slug) do update set status='ACTIVE',verification_state='VERIFIED',is_verified=true,verified_at=timezone('utc',now()) returning id into lab;
 insert into public.radiology_centers(legal_name,display_name_i18n,slug,description_i18n,country_id,city_id,address_i18n,status,verification_state,is_verified,verified_at) values('CareBridge Radiology Center - TEST','{"en":"CareBridge Radiology Center - TEST","ar":"مركز أشعة كيربريدج - اختبار"}','carebridge-radiology-test','{}',country,city,'{}','ACTIVE','VERIFIED',true,timezone('utc',now())) on conflict(slug) do update set status='ACTIVE',verification_state='VERIFIED',is_verified=true,verified_at=timezone('utc',now()) returning id into rad;
 insert into public.accommodation_properties(property_name,city_id,location_address,notes,is_active,created_by) values('CareBridge Recovery Hotel - TEST',city,'Fictional CareBridge Test Address','TEST accommodation option',true,admin_id) on conflict do nothing; select id into property from public.accommodation_properties where property_name='CareBridge Recovery Hotel - TEST' limit 1; insert into public.accommodation_room_options(property_id,room_type,available_rooms,price_per_night,currency,meal_plan,wellness_services,is_active,created_by) values(property,'Recovery Suite - TEST',8,240,'EUR','Half board',array['Recovery support'],true,admin_id) on conflict(property_id,room_type) do update set is_active=true returning id into room;
 insert into public.bookings(booking_reference,patient_id,status,journey_type,journey_timezone,journey_status,expected_start_date,expected_end_date,created_by,coordination_notes) values('CB-ADMINTEST26',patient,'PENDING_CONFIRMATION','INTERNATIONAL_MEDICAL_TRAVEL','Africa/Cairo','PLANNING','2026-11-10','2026-11-20',admin_id,'CARE JOURNEY ADMIN SELECTED - TEST') on conflict(booking_reference) do update set coordination_notes=excluded.coordination_notes returning id into admin_journey;
 insert into public.bookings(booking_reference,patient_id,status,journey_type,journey_timezone,journey_status,expected_start_date,expected_end_date,created_by,coordination_notes) values('CB-CHOICETEST26',patient,'PENDING_CONFIRMATION','INTERNATIONAL_MEDICAL_TRAVEL','Africa/Cairo','PLANNING','2026-12-10','2026-12-20',admin_id,'CARE JOURNEY PATIENT CHOICE - TEST') on conflict(booking_reference) do update set coordination_notes=excluded.coordination_notes returning id into choice_journey;
 insert into public.journey_services(booking_id,service_type,selection_state,status,title,doctor_id,base_cost,currency,carebridge_fee_percent,created_by,notes) select admin_journey,'DOCTOR_CONSULTATION','ADMIN_SELECTED','REQUESTED','Doctor consultation - TEST',d,300,'EUR',30,admin_id,'CARE JOURNEY ADMIN SELECTED - TEST' where not exists(select 1 from public.journey_services where booking_id=admin_journey and service_type='DOCTOR_CONSULTATION');
 insert into public.journey_services(booking_id,service_type,selection_state,status,title,hospital_id,treatment_id,base_cost,currency,carebridge_fee_percent,created_by,notes) select admin_journey,'HOSPITAL_PROCEDURE','ADMIN_SELECTED','REQUESTED','Surgery / procedure - TEST',h,t,8000,'EUR',30,admin_id,'CARE JOURNEY ADMIN SELECTED - TEST' where not exists(select 1 from public.journey_services where booking_id=admin_journey and service_type='HOSPITAL_PROCEDURE');
 insert into public.journey_services(booking_id,service_type,selection_state,status,title,medical_laboratory_id,base_cost,currency,carebridge_fee_percent,created_by,notes) select admin_journey,'LABORATORY','ADMIN_SELECTED','REQUESTED','Laboratory - TEST',lab,250,'EUR',30,admin_id,'CARE JOURNEY ADMIN SELECTED - TEST' where not exists(select 1 from public.journey_services where booking_id=admin_journey and service_type='LABORATORY');
 insert into public.journey_services(booking_id,service_type,selection_state,status,title,radiology_center_id,base_cost,currency,carebridge_fee_percent,created_by,notes) select admin_journey,'RADIOLOGY','ADMIN_SELECTED','REQUESTED','Radiology - TEST',rad,450,'EUR',30,admin_id,'CARE JOURNEY ADMIN SELECTED - TEST' where not exists(select 1 from public.journey_services where booking_id=admin_journey and service_type='RADIOLOGY');
 insert into public.journey_services(booking_id,service_type,selection_state,status,title,base_cost,currency,carebridge_fee_percent,created_by,notes) select choice_journey,'HOSPITAL_PROCEDURE','PATIENT_TO_CHOOSE','PLANNED','Surgery / procedure - TEST',8000,'EUR',30,admin_id,'CARE JOURNEY PATIENT CHOICE - TEST' where not exists(select 1 from public.journey_services where booking_id=choice_journey and service_type='HOSPITAL_PROCEDURE');
 insert into public.journey_accommodation_preferences(booking_id,patient_id,arrangement,notes,updated_by) values(admin_journey,patient,'CAREBRIDGE_ARRANGED','ADMIN_SELECTED / CareBridge-arranged accommodation - TEST',admin_id) on conflict(booking_id) do update set arrangement='CAREBRIDGE_ARRANGED',notes=excluded.notes,updated_by=admin_id; insert into public.accommodation_bookings(booking_id,patient_id,room_option_id,arrangement,check_in_date,check_out_date,number_of_rooms,guests,price_per_night,currency,status,created_by,notes) select admin_journey,patient,room,'CAREBRIDGE_ARRANGED','2026-11-10','2026-11-20',1,1,240,'EUR','HELD',admin_id,'CARE JOURNEY ADMIN SELECTED - TEST' where not exists(select 1 from public.accommodation_bookings where booking_id=admin_journey and status in('HELD','CONFIRMED','CHECKED_IN'));
 insert into public.journey_accommodation_preferences(booking_id,patient_id,arrangement,notes,updated_by) values(choice_journey,patient,'PATIENT_WILL_CHOOSE','PATIENT_TO_CHOOSE - TEST',admin_id) on conflict(booking_id) do update set arrangement='PATIENT_WILL_CHOOSE',notes=excluded.notes,updated_by=admin_id;
 insert into public.notifications(recipient_id,type,title_key,message_key,related_entity_type,related_entity_id) select patient,'journey.service.choice','journey.notifications.statusTitle','journey.notifications.statusMessage','booking',choice_journey where not exists(select 1 from public.notifications where recipient_id=patient and type='journey.service.choice' and related_entity_id=choice_journey);
 end $$; select b.booking_reference,b.coordination_notes,count(s.id) services,count(ii.id) finance_lines from public.bookings b left join public.journey_services s on s.booking_id=b.id left join public.invoice_items ii on ii.journey_service_id=s.id where b.booking_reference in('CB-ADMINTEST26','CB-CHOICETEST26') group by b.id order by b.booking_reference;`;
const post=async q=>{const r=await fetch(`https://api.supabase.com/v1/projects/${process.env.SUPABASE_PROJECT_REF}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${process.env.SUPABASE_ACCESS_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({query:q})});if(!r.ok)throw new Error(await r.text());return r.json()};
const rows=await post(query); if(!Array.isArray(rows)||rows.length!==2||rows.some(x=>Number(x.services)<1||Number(x.finance_lines)<1))throw new Error('Journey service TEST scenario preparation did not produce linked services and finance lines.'); console.log(JSON.stringify({ok:true,scenarios:rows},null,2));
