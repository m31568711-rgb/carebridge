import{readFileSync}from'node:fs';for(const line of readFileSync('.env.local','utf8').split(/\r?\n/)){const m=line.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);if(m&&!process.env[m[1]])process.env[m[1]]=m[2].replace(/^(['"])(.*)\1$/,'$2')}for(const k of['SUPABASE_PROJECT_REF','SUPABASE_ACCESS_TOKEN'])if(!process.env[k])throw new Error(`Missing ${k}`);const sql=async query=>{const r=await fetch(`https://api.supabase.com/v1/projects/${process.env.SUPABASE_PROJECT_REF}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${process.env.SUPABASE_ACCESS_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({query})});if(!r.ok)throw new Error(`SQL ${r.status}: ${await r.text()}`);return r.json()};const rows=await sql(`do $$ declare b public.bookings;a uuid;creator uuid;begin select * into b from public.bookings where booking_reference='CB-DEMO2026INTL';if b.id is null or b.doctor_id is null or b.hospital_id is null or b.treatment_id is null then raise exception 'Main persistent demo journey is incomplete';end if;creator:=coalesce(b.created_by,b.patient_id);select id into a from public.appointments where booking_id=b.id and appointment_type='TREATMENT_PROCEDURE' and status not in('CANCELLED','NO_SHOW') order by scheduled_at limit 1;if a is null then insert into public.appointments(booking_id,patient_id,provider_type,hospital_id,doctor_id,appointment_type,scheduled_at,timezone,duration_minutes,location_name,status,instructions,created_by)values(b.id,b.patient_id,'HOSPITAL',b.hospital_id,b.doctor_id,'TREATMENT_PROCEDURE','2026-09-16T09:00:00Z','Africa/Cairo',120,'CareBridge demo surgical suite','CONFIRMED','Fictional pre-procedure instructions for the client demonstration.',creator)returning id into a;end if;update public.journey_services set doctor_id=b.doctor_id,treatment_id=b.treatment_id,appointment_id=a,planned_date='2026-09-16',notes='Fictional pre-procedure instructions for the client demonstration.' where id=(select id from public.journey_services where booking_id=b.id and service_type='HOSPITAL_PROCEDURE' order by created_at limit 1);if not exists(select 1 from public.notifications where recipient_id=b.patient_id and type='procedure.scheduled' and related_entity_id=b.id)then insert into public.notifications(recipient_id,type,title_key,message_key,related_entity_type,related_entity_id)values(b.patient_id,'procedure.scheduled','part6.notifications.procedurescheduledTitle','part6.notifications.procedurescheduledMessage','booking',b.id);end if;end$$;select b.booking_reference,count(distinct a.id)filter(where a.appointment_type='TREATMENT_PROCEDURE'and a.status not in('CANCELLED','NO_SHOW')) procedures,count(distinct p.id)filter(where p.status in('ISSUED','COMPLETED')) prescriptions,count(distinct l.id) labs,count(distinct r.id) radiology,count(distinct f.id) followups from public.bookings b left join public.appointments a on a.booking_id=b.id left join public.prescriptions p on p.booking_id=b.id left join public.lab_orders l on l.booking_id=b.id left join public.radiology_orders r on r.booking_id=b.id left join public.clinical_follow_ups f on f.booking_id=b.id where b.booking_reference='CB-DEMO2026INTL'group by b.id;`);if(!rows[0]||Number(rows[0].procedures)<1||Number(rows[0].prescriptions)<1||Number(rows[0].labs)<1||Number(rows[0].radiology)<1||Number(rows[0].followups)<1)throw new Error('Persistent clinical demo is incomplete');console.log(JSON.stringify({ok:true,journey:rows[0]},null,2));
