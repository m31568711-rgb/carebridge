import{readFileSync}from'node:fs';
for(const line of readFileSync('.env.local','utf8').split(/\r?\n/)){const m=line.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);if(m&&!process.env[m[1]])process.env[m[1]]=m[2].replace(/^(['"])(.*)\1$/,'$2')}
for(const key of['SUPABASE_PROJECT_REF','SUPABASE_ACCESS_TOKEN'])if(!process.env[key])throw new Error(`Missing ${key}`);
const sql=async query=>{const response=await fetch(`https://api.supabase.com/v1/projects/${process.env.SUPABASE_PROJECT_REF}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${process.env.SUPABASE_ACCESS_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({query})});if(!response.ok)throw new Error(`SQL ${response.status}: ${await response.text()}`);return response.json()};
const rows=await sql(`do $$ declare b public.bookings;i uuid;creator uuid;begin select * into b from public.bookings where booking_reference='CB-DEMO2026INTL';if b.id is null or b.offer_id is null or b.provider_type is null then raise exception 'Persistent main demo journey is incomplete';end if;creator:=coalesce(b.created_by,b.patient_id);insert into public.invoices(invoice_number,booking_id,offer_id,patient_id,provider_type,hospital_id,pharmacy_id,radiology_center_id,medical_laboratory_id,currency,due_date,status,notes,created_by)values('CBI-FINDEMO2026A',b.id,b.offer_id,b.patient_id,b.provider_type,b.hospital_id,b.pharmacy_id,b.radiology_center_id,b.medical_laboratory_id,'USD',current_date+14,'ISSUED','Fictional CareBridge customer account demo.',creator)on conflict(invoice_number)do nothing;select id into i from public.invoices where invoice_number='CBI-FINDEMO2026A';insert into public.invoice_items(invoice_id,description,service_category,quantity,unit_amount,base_unit_amount,carebridge_fee_percent,display_order)values(i,'Fictional specialist consultation','DOCTOR',1,300,300,30,1),(i,'Fictional recovery accommodation','ACCOMMODATION',5,600,600,30,2),(i,'Fictional arrival coordination','TRAVEL',1,800,800,30,3)on conflict do nothing;insert into public.invoices(invoice_number,booking_id,offer_id,patient_id,provider_type,hospital_id,pharmacy_id,radiology_center_id,medical_laboratory_id,currency,due_date,status,notes,created_by)values('CBI-FINDEMO2026B',b.id,b.offer_id,b.patient_id,b.provider_type,b.hospital_id,b.pharmacy_id,b.radiology_center_id,b.medical_laboratory_id,'USD',current_date+21,'ISSUED','Fictional partially paid CareBridge service demo.',creator)on conflict(invoice_number)do nothing;select id into i from public.invoices where invoice_number='CBI-FINDEMO2026B';insert into public.invoice_items(invoice_id,description,service_category,quantity,unit_amount,base_unit_amount,carebridge_fee_percent,display_order)values(i,'Fictional laboratory program','LABORATORY',1,450,450,30,1),(i,'Fictional radiology review','RADIOLOGY',1,350,350,30,2)on conflict do nothing;if not exists(select 1 from public.payment_records where invoice_id=i and reference_number='CB-FIN-DEMO-PARTIAL')then insert into public.payment_records(invoice_id,booking_id,patient_id,amount,currency,paid_at,method,reference_number,recorded_by)values(i,b.id,b.patient_id,400,'USD',now()-interval '1 day','BANK_TRANSFER','CB-FIN-DEMO-PARTIAL',creator);end if;end$$;select invoice_number,status,total_amount,amount_paid,currency from public.invoices where invoice_number in('CBI-FINDEMO2026A','CBI-FINDEMO2026B') order by invoice_number;`);
if(rows.length!==2||Number(rows[0].total_amount)<=0||Number(rows[1].amount_paid)<=0)throw new Error('Customer finance demo data is incomplete');
console.log(JSON.stringify({ok:true,invoices:rows},null,2));
