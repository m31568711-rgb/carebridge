import {notFound} from '@/src/react-app/compat/navigation';import {AppointmentsPage} from '@/src/features/operations/appointments-page';import {isLocale} from '@/src/i18n/config';import {requireRoles} from '@/src/lib/auth/context';export const dynamic='force-dynamic';export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{page?:string;status?:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireRoles(locale,['DOCTOR']);return <AppointmentsPage locale={locale} portal="doctor" query={await searchParams}/>}
