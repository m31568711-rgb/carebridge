import {notFound} from 'next/navigation';import Link from 'next/link';import {Activity,CalendarClock,ClipboardCheck} from 'lucide-react';import {PageHeader} from '@/src/components/ui/page-header';import {StatCard} from '@/src/components/ui/stat-card';import {Card,CardContent} from '@/src/components/ui/card';import {Badge} from '@/src/components/ui/badge';import {getClinicalDictionary} from '@/src/features/clinical/messages';import {isLocale} from '@/src/i18n/config';import {requireRoles} from '@/src/lib/auth/context';import {getSupabaseServerClient} from '@/src/lib/supabase/server';export const dynamic='force-dynamic';
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireRoles(locale,['DOCTOR']);const s=await getSupabaseServerClient();if(!s)notFound();const[bookings,labs,radiology,followups]=await Promise.all([s.from('bookings').select('id,booking_reference,clinical_status,updated_at').order('updated_at',{ascending:false}).limit(100),s.from('lab_results').select('id').is('reviewed_at',null).not('released_at','is',null),s.from('radiology_results').select('id').is('reviewed_at',null).not('released_at','is',null),s.from('clinical_follow_ups').select('id').in('status',['PLANNED','SCHEDULED']).gte('recommended_date',new Date().toISOString().slice(0,10))]);const copy=getClinicalDictionary(locale),rows=bookings.data??[],awaiting=(labs.data?.length??0)+(radiology.data?.length??0);return <div className="mx-auto max-w-7xl"><PageHeader description={copy.description} eyebrow={copy.eyebrow} title={copy.encounters}/><div className="my-8 grid gap-4 sm:grid-cols-3"><StatCard hint={copy.description} icon={Activity} title={copy.activeJourneys} value={String(rows.filter(x=>!['COMPLETED','CANCELLED'].includes(x.clinical_status)).length)}/><StatCard hint={copy.patientRelease} icon={ClipboardCheck} title={copy.pendingReviews} tone="amber" value={String(awaiting)}/><StatCard hint={copy.recommendation} icon={CalendarClock} title={copy.upcomingFollowUps} tone="violet" value={String(followups.data?.length??0)}/></div><div className="grid gap-4 md:grid-cols-2">{rows.map(row=><Link href={`/${locale}/doctor/bookings/${row.id}`} key={row.id}><Card variant="interactive"><CardContent><div className="flex justify-between gap-3"><b className="font-mono">{row.booking_reference}</b><Badge variant="blue">{row.clinical_status}</Badge></div></CardContent></Card></Link>)}</div></div>}
