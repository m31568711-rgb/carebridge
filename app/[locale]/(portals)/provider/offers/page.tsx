import {notFound} from 'next/navigation';import {EmptyState} from '@/src/components/ui/empty-state';import {PageHeader} from '@/src/components/ui/page-header';import {OfferCard} from '@/src/features/journey/journey-ui';import {loadOffers} from '@/src/features/journey/data';import {getJourneyDictionary} from '@/src/features/journey/messages';import {isLocale} from '@/src/i18n/config';import {requireRoles} from '@/src/lib/auth/context';import {getSupabaseServerClient} from '@/src/lib/supabase/server';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();await requireRoles(locale,['PROVIDER','HOSPITAL_ADMIN','HOSPITAL_COORDINATOR','PHARMACY']);const copy=getJourneyDictionary(locale);const supabase=await getSupabaseServerClient();const offers=supabase?await loadOffers(supabase):[];return <div className="mx-auto max-w-7xl"><PageHeader description={copy.offers.providerDescription} eyebrow={copy.offers.eyebrow} title={copy.offers.providerTitle}/>{offers.length?<div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{offers.map(o=><OfferCard copy={copy} href={`/${locale}/provider/offers/${o.id}`} key={o.id} locale={locale} record={o}/>)}</div>:<div className="mt-8"><EmptyState title={copy.offers.empty}/></div>}</div>}
