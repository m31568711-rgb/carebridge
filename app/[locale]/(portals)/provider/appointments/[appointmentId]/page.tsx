import {notFound} from 'next/navigation';import {AppointmentDetailPage} from '@/src/features/operations/appointment-detail-page';import {isLocale} from '@/src/i18n/config';import {requireRoles} from '@/src/lib/auth/context';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{locale:string;appointmentId:string}>}){const{locale,appointmentId}=await params;if(!isLocale(locale))notFound();await requireRoles(locale,['PROVIDER','HOSPITAL_ADMIN','HOSPITAL_COORDINATOR','PHARMACY']);return <AppointmentDetailPage id={appointmentId} locale={locale} portal="provider"/>}
