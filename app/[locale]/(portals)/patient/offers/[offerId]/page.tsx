import {notFound} from 'next/navigation';import {OfferDetail} from '@/src/features/journey/journey-detail';import {loadOffer} from '@/src/features/journey/data';import {getJourneyDictionary} from '@/src/features/journey/messages';import {isLocale} from '@/src/i18n/config';import {requireRoles} from '@/src/lib/auth/context';import {getSupabaseServerClient} from '@/src/lib/supabase/server';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{locale:string;offerId:string}>}){const{locale,offerId}=await params;if(!isLocale(locale))notFound();await requireRoles(locale,['PATIENT']);const supabase=await getSupabaseServerClient();if(!supabase)notFound();const record=await loadOffer(supabase,offerId);if(!record||record.status==='DRAFT'||record.status==='WITHDRAWN')notFound();return <OfferDetail backHref={`/${locale}/patient/offers`} copy={getJourneyDictionary(locale)} locale={locale} mode="patient" record={record}/>}
