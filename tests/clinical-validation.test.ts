import {describe,expect,it} from 'vitest';import {encounterSchema,followUpSchema,orderSchema,prescriptionSchema,resultSchema} from '@/src/features/clinical/validation';
const id='3b241101-e2bb-4255-8caf-4136c566a962';
describe('Part 6 clinical validation',()=>{
 it('accepts a structured private encounter and limits status',()=>{const value={booking_id:id,encountered_at:'2026-09-10T10:00',clinical_notes:'Fictional private note',assessment:'Stable',diagnosis_summary:'Demo only',treatment_progress:'Improving',next_steps:'Continue',follow_up_recommendation:'Review',status:'COMPLETED'};expect(encounterSchema.safeParse(value).success).toBe(true);expect(encounterSchema.safeParse({...value,status:'PUBLISHED'}).success).toBe(false);});
 it('requires normalized medication directions',()=>{const value={booking_id:id,medication_name:'CareBridge Demo Medicine',strength:'10 demo units',dosage:'One unit',frequency:'Once daily',route:'Oral',duration:'5 days',instructions:'Demo only',notes:'',item_instructions:'',start_date:'2026-09-10',end_date:'2026-09-15',dose_times:'08:00'};expect(prescriptionSchema.safeParse(value).success).toBe(true);expect(prescriptionSchema.safeParse({...value,dosage:''}).success).toBe(false);});
 it('validates diagnostic priority and names',()=>{const value={booking_id:id,provider_id:id,priority:'ROUTINE',requested_name:'CareBridge Demo Panel',modality:'',body_area:'',clinical_instructions:''};expect(orderSchema.safeParse(value).success).toBe(true);expect(orderSchema.safeParse({...value,priority:'EMERGENCY'}).success).toBe(false);});
 it('prevents an inverted follow-up window and constrains release',()=>{const follow={booking_id:id,recommended_date:'2026-09-20',window_end_date:'2026-09-15',recommendation:'Fictional follow-up'};expect(followUpSchema.safeParse(follow).success).toBe(false);expect(resultSchema.safeParse({order_id:id,kind:'lab',summary:'Demo',notes:'',release:'yes'}).success).toBe(true);expect(resultSchema.safeParse({order_id:id,kind:'lab',summary:'Demo',notes:'',release:'public'}).success).toBe(false);});
});
